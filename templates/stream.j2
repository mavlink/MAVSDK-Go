
func (a *ServiceImpl) {{ name.upper_camel_case }}({% for param in params %}, {{ param.name.lower_snake_case }}{% endfor %}){
	request := &Subscribe{{ name.upper_camel_case }}Request{}
		ctx := context.Background()
		stream, err := a.Client.Subscribe{{ name.upper_camel_case }}(ctx, request)
		if err != nil {
			fmt.Printf("Unable to subscribe %v\n", err)
		}

		for {
			m := &{{ name.upper_camel_case }}{}
			err := stream.RecvMsg(m)
			if err == io.EOF {
				break
			}
			if err != nil {
				fmt.Printf("Unable to receive message %v", err)
				break
			}
			fmt.Printf("message %v\n", m.Get{{name.upper_camel_case}}())
		}	
}
